import activeSlider from "../assets/activeSlider.svg";
import home from "../assets/home.svg";
import profile from "../assets/profile.svg";

export const configSvg = () => {
  return { home, activeSlider, profile };
};
